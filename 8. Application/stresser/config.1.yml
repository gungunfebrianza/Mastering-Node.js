config:
  target: 'http://marketkoin.com'
  phases:
    # We define first load phase, which will last 60 seconds with 25 new virtual users (arriving every second (on average).
    - duration: 60
      arrivalRate: 25
    - duration: 60
      arrivalRate: 25
      rampTo: 200
    - duration: 600
      arrivalRate: 250
scenarios:
  - flow:
      - get:
          url: '/'
